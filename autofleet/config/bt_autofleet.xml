<root main_tree_to_execute="MainTree">
    <BehaviorTree ID="MainTree">
        <Sequence name="AutofleetControl">
            <FormTeamState name="FormTeamState" output_last_follow_poses_ptr = "{last_follow_poses_ptr}"/>
            <MoveState name="MoveState" input_last_follow_poses_ptr = "{last_follow_poses_ptr}"/>
            <BreakTeamState name="BreakTeamState" />
        </Sequence>
    </BehaviorTree>
</root>